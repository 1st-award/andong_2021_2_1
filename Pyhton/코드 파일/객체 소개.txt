객체: 속성과 기능을 가지고 있는 일반적인 실체

클래스 = 붕어빵 틀: 설계도 혹은 틀과 같다. 실체는 아니다. 메소드로 구성되어있다.
붕어빵 = 인스턴스(객체): 클래스기반으로 구현된 실체이다. 구체화된 인스턴스는 메모리에 할당된다.
메소드 = 객체의 기능(메소드는 함수랑 비슷하다)
생성자 메소드명: def __init__(self, filed1, filed2...): # self는 반드시 들어감